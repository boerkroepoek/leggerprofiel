# Leggerprofiel Streamlit

Interactieve herbouw van de profielberekening uit het aangeleverde Excel-bestand.

## Starten

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```

## Testen

```bash
python -m pytest -q
```
